# P-value combination method

P-value combination method

## Usage

``` r
p_combine(p, R, method = c("fisher", "stouffer"))
```

## Arguments

- p:

  p-values (numeric/vector)

- R:

  correlation matrix

- method:

  choose between 'fisher' or 'stouffer' methods
